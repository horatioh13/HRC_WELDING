from setuptools import setup

# Package meta-data.
NAME = 'URBasic'
DESCRIPTION = 'Modified Python bindings for the UR RTDE.'
VERSION = '1.0.0'


setup(name=NAME,
      version=VERSION,
      description=DESCRIPTION,
	packages=[NAME],
)
